{
  "schema_version": "pvrig_v2_11_canonical_multimodal_fusion_contract_v1",
  "status": "PREREGISTERED_BEFORE_CANONICAL1358_PERFORMANCE_COMPUTATION",
  "claim_boundary": "Open-development approximation of independent 8X6B/9E6Y computational Docking geometry only; not binding, affinity, experimental blocking, Docking Gold, frozen-test, sealed truth, or submission evidence.",
  "data": {
    "primary_intersection": "V2.10 canonical teacher intersect V4-D/V4-H 126D monomer structure, C2 coarse-pose, and ESM2-650M cache",
    "expected_canonical_rows": 1358,
    "expected_train_rows": 1282,
    "expected_development_rows": 76,
    "split_unit": "parent_framework_cluster",
    "development_truth_used_for_fit_or_selection": false,
    "variable_row_runner": true
  },
  "base_models": {
    "S0_MATCHED_ESM2_650M_PCA_ELASTICNET": {
      "inputs": "frozen ESM2-650M pooled cache plus sequence/CDR physicochemical features",
      "pca": "fold-local randomized PCA, up to 128 components, whitened",
      "regressor": "two ElasticNet heads; alpha=0.003, l1_ratio=0.10",
      "outputs": "direct R8/R9; exact-min Rdual"
    },
    "M2_STRUCTURE_ALPHA10": {
      "inputs": "126 label-free rigid-invariant VHH monomer descriptors",
      "regressor": "sample-weighted standardized Ridge alpha=10",
      "outputs": "direct R8/R9; exact-min Rdual"
    },
    "C2_COARSE_POSE_PCA8": {
      "inputs": "32 non-QC fields from the frozen 36D 8X6B/9E6Y coarse-pose scan",
      "transform": "fold-local standardize, constant filter, PCA8",
      "ridge_alpha_grid": [0.0001, 0.001, 0.01, 0.1, 1.0, 10.0, 100.0],
      "selection": "minimum train whole-parent OOF parent-macro mean MAE across R8/R9/exact-min Rdual; ties choose largest alpha"
    }
  },
  "meta_models": {
    "M2_C2_CONVEX": "M2 fallback plus nonnegative C2 residual weight, shared across receptors, total challenger weight <=1, L2=0.001",
    "S0_M2_C2_CONVEX": "M2 fallback plus nonnegative S0/C2 residual weights, shared across receptors, total challenger weight <=1, L2=0.001",
    "SHALLOW_GBDT_CHALLENGER": {
      "role": "challenger_only_not_primary",
      "features": "OOF base R8/R9, receptor gaps, and pairwise base disagreement only",
      "max_depth": 2,
      "max_iter": 64,
      "learning_rate": 0.05,
      "min_samples_leaf": 64,
      "l2_regularization": 2.0
    }
  },
  "full9849_stage0": {
    "role": "independent frozen base comparator only",
    "model": "ELASTICNET_ESM2_650M_PCA three-seed ensemble",
    "used_to_fit_fusion_weights": false,
    "used_for_hyperparameter_selection": false
  },
  "evaluation": {
    "primary": [
      "true Top10% enrichment at predicted budget10%",
      "true Top20% recall at predicted budget20%",
      "within-parent Top20 macro recall"
    ],
    "secondary": ["Rdual Spearman", "Rdual MAE", "Rdual RMSE", "R8/R9 Spearman and MAE"],
    "rounding": "ceil",
    "tie_break": "score descending then candidate_id ascending",
    "development_only": true,
    "no_formal_or_production_promotion_from_dev76_alone": true
  },
  "prohibitions": [
    "frozen-test or sealed-truth access",
    "candidate Docking pose as predictor",
    "candidate, parent, source, campaign, or split ID as predictor",
    "same-row base predictions for meta-head fitting",
    "development labels for alpha, fusion weight, or GBDT fitting",
    "direct independent Rdual output",
    "post-result contract changes under the same version"
  ]
}
