# Final submission deliverables

`final_top50_ranked.tsv` is the full 50-row evidence master.  Its paired FASTA
contains the same 50 candidates.  `final50.official_submit.xlsx` is the official
50-row Excel export.  `candidate_traceability.tsv` is the concise provenance
matrix; `top10_priority.*` lists the representatives whose 9E6Y PDBs appear in
`evidence/top10_poses/`.
