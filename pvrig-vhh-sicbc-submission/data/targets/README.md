# Target inputs and restraint example

`8X6B.pdb` and `9E6Y.pdb` are the two public receptor conformations used for
dual-conformation analysis.  The CSV files record interface/contact information.
`pvrig_hotspot_example.cfg` is a compact HADDOCK hotspot configuration example,
not a complete docking run directory.
