# Compact provenance

`positive11_cdr_imgt.tsv` is the parent-CDR authority used to derive CDR
modifications in the final trace table.  `model_weights_manifest.tsv` gives
model version, SHA256 where captured, and acquisition instructions.
`SOURCE_ARTIFACTS.md` points to the non-copied source tables needed to rebuild
the final-only traceability artifact.
