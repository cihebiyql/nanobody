# Submission scripts

- `build_traceability.py`: rebuilds the final 50-row provenance matrix from the
  two named upstream score tables and compact final evidence.
- `write_sha256s.py`: writes a deterministic integrity list.
- `verify_submission.py`: CPU-only validation of counts, trace fields, PDBs,
  exclusions, and checksums.
