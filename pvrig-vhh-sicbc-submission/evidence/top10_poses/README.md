# Top10 9E6Y representative poses

There is exactly one PDB per Top10 priority candidate.  Its filename embeds the
candidate ID, 9E6Y conformation, and docking seed.  Refer to
`data/submission/candidate_traceability.tsv` for the paired 8X6B pose, protocol
hash, score fields, and review status.
