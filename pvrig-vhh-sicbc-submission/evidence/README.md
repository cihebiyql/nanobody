# Representative structural evidence

`top10_poses/` contains one 9E6Y representative complex PDB for each priority
Top10 candidate.  Each filename embeds its candidate ID and docking seed.
`manifests/` stores SHA256 checksum inventories.  Full docking trees and all
poses are excluded by design.
