# Tests

`test_submission_integrity.py` executes the same package-level verifier used by
reviewers.  Run `pytest -q` after installing the lightweight requirements, or
invoke `python3 scripts/verify_submission.py` where pytest is unavailable.
