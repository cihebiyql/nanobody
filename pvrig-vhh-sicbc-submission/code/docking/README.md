# Target preparation and docking

`extract_pvrig_interface.py` prepares 8X6B/9E6Y interface information.
The HADDOCK package, shard controller, post-processing, and consensus scripts
are retained here.  Only a representative hotspot configuration and ten final
poses are included; complete docking work directories are deliberately absent.
