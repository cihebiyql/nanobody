# Multimodal model code

Input materialization, structure/contact feature extraction, training,
evaluation, inference delivery, and late-fusion scoring are collected here.
The source files retain their original versioned names to keep the provenance
clear.  Fitted checkpoints and large training matrices are intentionally not
bundled; see `data/provenance/model_weights_manifest.tsv`.
