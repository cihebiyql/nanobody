# Requirements

`requirements.txt` is the lightweight Python dependency set.  `environment.md`
documents the external scientific tools and their non-vendored boundary; it is
not a Conda environment dump.
