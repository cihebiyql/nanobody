# 本地查看副本

本地已包含 Final50/Top10 FASTA、完整排序表，以及 50 条候选在 4 个随机 seed × 2 个受体构象下的 400 个代表 HADDOCK 复合物 PDB。

未复制 docking/archives/ 中的 400 个原始归档（约 379 MB）；它们只保存全套 10 个 selected cluster、日志及配置，仍在 Node1。每个归档的远端绝对路径及 SHA256 见 docking/representative_models_manifest.tsv。

PDB 为计算 docking 结构，不代表实验结合、亲和力或阻断证明。
